# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: Name
        searchable: true

    code:
        label: Code
        searchable: true

    is_enabled:
        label: Enabled
        type: switch

    is_pinned:
        label: Pinned
        type: switch

    calling_code:
        label: Calling Code
        searchable: true
        invisible: true
